@model IEnumerable<iiifly.Models.SpaceMapping>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DlcsSpace)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SpaceId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DlcsSpace)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SpaceId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SpaceMappingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SpaceMappingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SpaceMappingId })
        </td>
    </tr>
}

</table>
